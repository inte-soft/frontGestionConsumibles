export class Folder {
    id: string;
    ot: number;
    name: string;

    constructor(id: string, ot: number, name: string) {
        this.id = id;
        this.ot = ot;
        this.name = name;
    }
}